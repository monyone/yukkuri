# yukkuri

yukkuri is (partial, imcomplete) SRT implementation written in Pure TypeScript for using by any JS environment (UDP on node.js, WebTransport Datagram on Browser).

## Feature

* Read SRT stream using caller mode

## Build

```bash
yarn
yarn build
```

